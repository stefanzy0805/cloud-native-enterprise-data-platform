# notebooks

这个目录保存 Jupyter Notebook。

现有 notebook 覆盖 Python、SQL、Spark、Terraform 和企业级数据平台设计等学习与实验内容。它们适合用于探索、演示、复习和验证思路。

生产级逻辑应逐步沉淀到 `ingestion/`、`etl/`、`data_quality/` 或 `orchestration/`，避免只停留在 notebook 中。
